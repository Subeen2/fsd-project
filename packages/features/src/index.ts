/**
 * @fsd/features
 *
 * Shared business logic including custom hooks and service abstractions.
 * Add feature-specific subdirectories as the project grows:
 *
 *   src/
 *   ├── auth/          useAuth, authService
 *   ├── posts/         usePosts, usePost, postService
 *   ├── users/         useUser, userService
 *   └── websocket/     useWebSocket, wsService
 */

// Placeholder exports — replace with actual feature modules
export const FEATURES_VERSION = "0.0.1";
